--!strict

local CombatService = {}

function CombatService.Init()
    -- Setup references, no yielding
end

function CombatService.Start()
    -- Connect to Ensemble.Events, start loops
end

return CombatService